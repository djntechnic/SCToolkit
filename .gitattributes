# Normalize to LF in the repository. The built userscript is compared against a
# fresh build in CI (Linux), so line endings must not depend on the committer's
# platform.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
